
import CreateCarScreen from 'components/screens/CreateCarScreen';
import { View } from 'react-native';

export default function CreateCar() {
  return (
    <View className='p-4'>
      <CreateCarScreen />
    </View>
  );
}
